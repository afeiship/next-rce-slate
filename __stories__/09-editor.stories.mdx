import { Meta } from '@storybook/addon-docs/blocks';
import { Editor, Toolbar } from '../src/main.ts';
import InlineStyle from '../src/components/plugins/inline-style';
import Bold from '../src/components/plugins/bold';
import FontMark from '../src/components/plugins/font-mark';
import './style.scss';

<Meta title="Example/09-editor" />

## showtime

<section>
  <Toolbar>
    Toolbar
  </Toolbar>

  <Editor
    plugins={[ InlineStyle, FontMark ]}
    onChange={e=>{
      console.log(e.target);
    }}
    value={[
      {
        type: 'paragraph',
        children: [
          { text: 'A line of text in a paragraph.' },
          { text: '' }
        ]
      }
    ]}
  />
</section>

