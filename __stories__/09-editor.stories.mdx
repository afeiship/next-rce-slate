import { Meta } from '@storybook/addon-docs/blocks';
import { Editor, Toolbar } from '../src/main.ts';
import FontMark from '../src/components/plugins/font-mark';
import Image from '../src/components/plugins/image';
import Latex from '../src/components/plugins/latex';
import './style.scss';

<Meta title="Example/09-editor" />

## showtime
<section>
  <Toolbar>
    Toolbar
  </Toolbar>

  <Editor
    plugins={[ FontMark, Image ]}
    onChange={e=>{
      console.log(e.target);
    }}
    value={`<p>A <span style="text-decoration: line-through;">line</span> of <span style="font-weight: bold;">text</span> in a <span style="font-weight: bold;">paragraph</span>.</p><p>sldfjsdfjsdf</p><p>sldflsdjsdf</p>`}
  />
</section>

